<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.denizalp.thefirst">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ObtainInfo" />
        <activity android:name=".UserPage" />
        <activity android:name=".TopicPage" />
        <activity android:name=".TopicAddMaterialQuiz" />
        <activity android:name=".EditUserInformationPage" />
        <activity android:name=".ShowTopicPage" />
        <activity android:name=".HomePage" />
        <activity android:name=".TopicListPage"></activity>
    </application>

</manifest>